# news-recap-site

A static site that publishes a daily news recap. `index.html` is overwritten each morning by an external scheduled task with the day's recap, and a dated copy is added to `archive/YYYY-MM-DD.html` so past days remain browsable via `archive/index.html`. Hosted on Cloudflare Pages.
